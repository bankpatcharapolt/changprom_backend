# แก้ไขรอบที่ 2 — บั๊ก validate หน้าจัดการพนักงาน + ปุ่มลบในหน้ารายการงาน

ไม่มี SQL migration ใหม่รอบนี้ — เป็นการแก้โค้ด PHP/JS ล้วนๆ อัปโหลดไฟล์ทับได้เลย

## 1) แก้บั๊ก "Column 'email' cannot be null"

**สาเหตุที่แท้จริง**: คอลัมน์ `email` ในตาราง `users` เป็น `NOT NULL` และมี `UNIQUE` อยู่แล้ว
(ยืนยันจาก `sql/schema.sql`) แต่ `Employee_model::_clean()` เดิม fallback เป็น `null` เวลาไม่กรอกอีเมล
ทำให้ insert พังทันทีที่เว้นว่าง — ทดสอบแล้วว่า reproduce error เดิมได้ตรงเป๊ะกับที่แจ้งมา

**วิธีแก้**: ทำให้ เบอร์โทร และ อีเมล เป็นฟิลด์บังคับ (ตรงกับที่พี่แก้ label ใส่ * ไว้เองในไฟล์ view
อยู่แล้ว) ทั้ง client-side และ server-side จึงไม่มีทางส่งค่าว่างไปถึง DB อีก — ไม่ต้องแก้ schema

**เพิ่มมาด้วย (สำคัญ)**: เดิม `create()`/`update()` ไม่เช็คผลลัพธ์จริงจาก DB เลย ต่อให้ insert/update
ล้มเหลว โค้ดก็ยังตอบ `success: true` กลับไปที่หน้าเว็บ (นี่คือสาเหตุที่ "มันไม่ยอมแจ้งเตือน" ตามที่แจ้งมา)
ตอนนี้เช็คผลลัพธ์จริงจาก DB แล้ว ถ้าล้มเหลวจะตอบ error กลับไปแสดงเป็น Swal popup เสมอ พร้อม log
รายละเอียดไว้ที่ server (`application/logs`) ให้ debug ต่อได้

## 2) หน้า View พนักงาน — เรื่องขอดู password

**ทำให้ตามที่ขอเป๊ะๆ ไม่ได้ครับ** — เหตุผลทางเทคนิค: password ถูกเข้ารหัสแบบ one-way (bcrypt ผ่าน
`password_hash()`) ตั้งแต่ตอนบันทึก ซึ่งเป็นวิธีเดียวกับที่ admin/staff/ช่างทุกบัญชีในระบบใช้อยู่แล้ว
แปลว่าค่าจริงไม่ได้ถูกเก็บไว้ที่ไหนเลยแม้แต่ในระบบเอง กู้คืนดูภายหลังไม่ได้จริงๆ (ถ้าเปลี่ยนไปเก็บแบบ
กู้คืนได้ จะลดความปลอดภัยของทั้งระบบลงมาก ไม่แนะนำ)

สิ่งที่ทำแทน: หน้า View จะบอกว่า "ตั้งรหัสผ่านไว้แล้ว" พร้อมปุ่ม "ตั้งรหัสผ่านใหม่" ที่พาไปหน้าแก้ไขทันที
(กรอกรหัสผ่านใหม่ให้พนักงานได้เลยถ้าจำเป็น เช่น ลืมรหัสผ่าน)

## 3) Validate ทุก field (เพิ่ม/แก้ไขพนักงาน)

Validate ทั้ง client-side (`employee.js`) และ server-side (`Employee.php::_validate()`) ตรงกันทุกจุด:
- ชื่อ/นามสกุล: บังคับกรอก, ไม่เกิน 100 ตัวอักษร
- Username: บังคับ, 3-100 ตัวอักษร, เฉพาะ `A-Z a-z 0-9 _ . -` (ห้ามเว้นวรรค/ภาษาไทย), ห้ามซ้ำ
- Password: บังคับตอนสร้างใหม่ (แก้ไขเว้นว่างได้ = ไม่เปลี่ยน), อย่างน้อย 6 ตัวอักษร
- เบอร์โทร: บังคับ, ต้องขึ้นต้น 0 และเป็นตัวเลข 9-10 หลัก (รองรับที่มีขีดคั่น)
- อีเมล: บังคับ, ต้องเป็นรูปแบบอีเมลที่ถูกต้อง, ห้ามซ้ำ (ตรงกับ UNIQUE constraint ที่มีอยู่แล้ว)
- ตำแหน่ง: ไม่บังคับ แต่ไม่เกิน 150 ตัวอักษร

## 4) หน้ารายการงาน (`/service`) — ปุ่มลบเฉพาะ superadmin

- `service.js`: ปุ่มลบ (ถังขยะ) แสดงเฉพาะ role = superadmin
- เพิ่มการเช็คฝั่ง server ด้วย (`Api::delete_service()`) กันไม่ให้ role อื่นเรียก
  `DELETE /api/service/{id}` ตรงๆ ได้ (ซ่อนปุ่มอย่างเดียวไม่พอ เพราะเรียก endpoint ตรงๆ ผ่าน
  browser dev tools ได้อยู่ดีถ้าไม่เช็คฝั่ง server ด้วย)

## ไฟล์ที่แก้ไขรอบนี้
- `application/models/Employee_model.php`
- `application/controllers/Employee.php`
- `application/views/employee/index.php`
- `assets/js/employee.js`
- `application/controllers/Service.php`
- `application/views/service/index.php`
- `assets/js/service.js`
- `application/controllers/Api.php`

## ทดสอบก่อนหน้านี้
ผมสร้างฐานข้อมูลทดสอบจำลอง schema เดียวกับ `sql/schema.sql` (users มี email เป็น NOT NULL + UNIQUE)
แล้ว reproduce error เดิมได้ตรงเป๊ะ จากนั้นทดสอบ flow ใหม่ (มี email/phone ครบ, สร้างซ้ำด้วย email ซ้ำ)
ยืนยันว่า error เดิมไม่เกิดอีก และกรณี email ซ้ำจะถูกจับด้วยข้อความที่เข้าใจง่ายก่อนถึง DB
